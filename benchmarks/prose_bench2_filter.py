# Auto-generated by prose transpiler — do not edit by hand
import sys, os, time, math, random
try:
    import urllib.request, json
except ImportError:
    pass

# ── prose standard-library polyfills ────────────────────
def _plain_say(*parts):
    print(''.join(str(p) for p in parts))
def _plain_ask(prompt=''):
    return input(prompt if prompt else '')
def _plain_len(obj): return len(obj) if hasattr(obj,'__len__') else 0
def _plain_type(obj, kind):
    if kind == 'number': return isinstance(obj,(int,float)) and not isinstance(obj,bool)
    if kind == 'text': return isinstance(obj,str)
    if kind == 'list': return isinstance(obj,list)
    if kind == 'boolean': return isinstance(obj,bool)
    return False
# prose stdlib aliases (matches interpreter behaviour)
time_now = time.time
math_sin = math.sin; math_cos = math.cos; math_tan = math.tan
math_log = math.log; math_log10 = math.log10; math_exp = math.exp
math_pi = math.pi
random_int = random.randint; random_float = random.random
# ─────────────────────────────────────────────────────────

import time
numbers = []
for i in range(int(0), int(9999) + 1, int(1)):
    numbers.append(i)
start = time_now()
passing = [n for n in numbers if (n > 8000)]
end = time_now()
_plain_say('prose Filtering:')
c = _plain_len(passing)
_plain_say((str((str('Found ') + str(c))) + str(' numbers')))
diff = (end - start)
_plain_say('Time taken (seconds):')
_plain_say(diff)
